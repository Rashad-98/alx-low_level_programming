#include <stdio.h>

/**
 * main - program that prints the number of arguments passed to it
 * @argc: arguments count
 *
 * Return: 0 always
 */
int main(int argc)
{
	printf("%d\n", argc - 1);

	return (0);
}
