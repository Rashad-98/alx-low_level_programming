#include "main.h"

/**
 * _islower - checks for lower case letters
 *@c: the character to pirnt
 *
 * Return: 1 if lowercase, 0 otherwise
 */
int _islower(int c)
{
	if (c >= 97 && !(c > 122))
	{
		return (0);
	}
	else
	{
		return (0);
	}
}
